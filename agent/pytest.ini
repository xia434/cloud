[pytest]
# 自定义标记注册（避免 PytestUnknownMarkWarning）
markers =
    slow: 慢速集成测试（依赖 LLM/Milvus 等外部服务），用 `pytest -m "not slow"` 跳过
# 默认从 tests/ 目录收集用例
testpaths = tests
# 严格的标记模式：未注册的标记直接报错，强制维护 markers 列表
addopts = --strict-markers -m "not slow"
